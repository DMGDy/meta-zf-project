# Base nginx configuration for embedded server
events {
  worker_connections 4;
}

http {
  server {
    listen 80;
    listen [::]:80;

    location / {
      root /var/www/html;
      index index.html;
    }
      #server_name _;
  }
}
